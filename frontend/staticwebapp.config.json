{
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous", "authenticated"],
      "methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
      "rewrite": "http://userapi09041259.swedencentral.azurecontainer.io:8080/api/"
    }
  ],
  "platformErrorOverrides": [
    {
      "errorType": "NotFound",
      "serve": "/index.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html"
  }
}